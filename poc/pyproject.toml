[project]
name = "kpmg-a2ui-poc"
version = "0.1.0"
description = "Local A2UI sample agent for KPMG widgets — run with adk web."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=1.0.0",
    "a2ui-agent-sdk>=0.1.2",
    "python-dotenv>=1.1.0",
]

[tool.uv]
native-tls = true
